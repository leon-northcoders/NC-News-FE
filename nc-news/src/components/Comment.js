import React from 'react';
import PT from 'prop-types';

function Comment (props) {
    return (
        <div>
            Comment
        </div>
    );
}

Comment.propTypes = {
    
}

export default Comment;